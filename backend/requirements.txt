# Core API framework
fastapi
uvicorn[standard]

# Environment variable loading
python-dotenv

# Groq SDK — financial data parsing + AI analysis (free)
# Get key: https://console.groq.com
groq

# File upload support
python-multipart

# Data validation
pydantic

# PDF extraction — Strategy 1 (text-based PDFs, fast)
pymupdf

# Google Gemini SDK — Strategy 2 (scanned PDF OCR via Vision, needs API key)
# Get key: https://aistudio.google.com/app/apikey
google-generativeai

# Tesseract OCR — Strategy 3 (local scanned PDF fallback)
# Requires Tesseract binary installed separately:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Mac:     brew install tesseract poppler
#   Ubuntu:  sudo apt install tesseract-ocr poppler-utils
pytesseract
pdf2image

# Excel and CSV parsing
pandas
openpyxl

# PDF report generation
fpdf2